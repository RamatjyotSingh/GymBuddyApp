<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymBuddy"
        android:name=".GlobalApplication"
        >

        <activity
            android:name="comp3350.gymbuddy.presentation.activity.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GymBuddy.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="comp3350.gymbuddy.presentation.activity.WorkoutBuilderActivity"
            android:exported="true"
            android:theme="@style/Theme.GymBuddy.NoActionBar"
            android:configChanges="orientation|screenSize"
           >

        </activity>

        <activity android:name=".presentation.activity.ExerciseListActivity"
            android:configChanges="orientation|screenSize"
            />

        <activity android:name=".presentation.activity.ExerciseDetailActivity" android:configChanges="orientation|screenSize"
            />

        <activity
            android:name="comp3350.gymbuddy.presentation.activity.WorkoutLogActivity"
            android:exported="true"
            android:theme="@style/Theme.GymBuddy.NoActionBar"
            android:configChanges="orientation|screenSize"
            >
        </activity>

        <activity
            android:name="comp3350.gymbuddy.presentation.activity.WorkoutLogDetailActivity"
            android:exported="true"
            android:theme="@style/Theme.GymBuddy.NoActionBar"
            android:configChanges="orientation|screenSize"
            >
        </activity>

            </application>

        </manifest>